# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: tests\api\acct & balance validation.spec.ts >> acct & balance validation
- Location: tests\tests\api\acct & balance validation.spec.ts:3:5

# Error details

```
TypeError: request.get is not a function
```

# Test source

```ts
  1  | import {APIRequestContext} from "@playwright/test";
  2  | import {loginData} from "../test-data/logindata.json";
  3  | export async function getCustomerId(request:APIRequestContext, username:string, password:string) {
> 4  |     const response = await request.get(`https://parabank.parasoft.com/parabank/services/bank/login/${username}/${password}`);
     |                                    ^ TypeError: request.get is not a function
  5  |     const body = await response.text();
  6  |     console.log("Login API Response :");
  7  |     console.log(body);
  8  |     const customerId = body.match(/<id>(\d+)<\/id>/)?.[1];
  9  |     console.log("Extracted Customer ID :", customerId);
  10 |     return customerId;
  11 | }
```